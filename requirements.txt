# ── 웹 서버 ──────────────────────────────────────
flask
werkzeug
jinja2
itsdangerous
starlette
fastapi
uvicorn[standard]
python-multipart

# ── DB ───────────────────────────────────────────
psycopg2-binary

# ── AI / 이미지 분석 ──────────────────────────────
# torch는 Dockerfile에서 CPU 버전으로 먼저 설치됨
# 로컬 실행 시: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
open_clip_torch
transformers
timm
pillow
numpy
scipy
opencv-python
ftfy

# ── 객체 탐지 (YOLO) ─────────────────────────────
ultralytics

# ── 이미지 클라우드 저장 ───────────────────────────
cloudinary==1.44.1

# ── 인증 (로그인) ─────────────────────────────────
Flask-Login==0.6.3
authlib==1.6.10

# ── AI 추천 (Claude API) ──────────────────────────
anthropic
openai

# ── 날씨 API / 환경변수 ───────────────────────────
requests
python-dotenv

# ── 흐름도 생성 ───────────────────────────────────
matplotlib

# ── HTTP 클라이언트 ───────────────────────────────
httpx

# ── 음성 (STT) ────────────────────────────────────
openai-whisper

# ── 데이터 처리 ───────────────────────────────────
pandas
polars
pydantic
PyYAML
huggingface_hub
tqdm
psutil
rich

# ── 누적 학습 파이프라인 (2026-04-17 추가) ─────────
# retrain_classifier.py + training/ 폴더가 사용
# - pgvector       : Postgres에 이미지 임베딩 저장/유사도 검색
# - scikit-learn   : 경량 분류기(LogisticRegression/MLP) 학습
# - lightgbm       : 선택적 고성능 분류기 (train.py --model lightgbm)
# - joblib         : 학습된 모델 직렬화
# - pyarrow        : pandas.to_parquet 백엔드 (training/datasets/*.parquet)
pgvector
scikit-learn
lightgbm
joblib
pyarrow
